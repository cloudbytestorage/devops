package com.automaton.types.construct

enum SettingsConstructType {
    /*
     * Property to map success or failure; required after parsing the settings construct
     */
    status,
    /*
     * Property to map success or failure message; required after parsing the settings construct
     */
    msg
}
