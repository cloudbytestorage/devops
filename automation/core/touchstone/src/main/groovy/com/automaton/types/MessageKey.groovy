package com.automaton.types

enum MessageKey implements BaseType{

    msg,
    status,
    suggest,
    warnings,
    constructs,
    success,
    failed,
    latency,
    start_time,
    end_time
}
