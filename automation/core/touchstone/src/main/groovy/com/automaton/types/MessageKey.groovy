package com.automaton.types

enum MessageKey implements BaseType{

    msg,
    hasErrors,
    status,
    suggest,
    warnings,
    constructs,
    success,
    failed,
    latency,
    startTime,
    endTime
}
